The only reason why I need the QUPC_AR004030UV_Service.wsdl is because the 
JBoss generated client proxy use it.
More precisely if you want to call a wsld based service using the generated files
you need to specifiy the WSLD file location.